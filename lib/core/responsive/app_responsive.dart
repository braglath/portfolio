class AppResponsive {
  AppResponsive._();

  // void init(BuildContext context) {
  // // Booleans
  // ResponsiveBreakpoints.of(context).isDesktop;
  // ResponsiveBreakpoints.of(context).isTablet;
  // ResponsiveBreakpoints.of(context).isMobile;
  // // ResponsiveBreakpoints.of(context).;

  // // Conditionals
  // ResponsiveBreakpoints.of(context).void equals(DESKTOP);
  // ResponsiveBreakpoints.of(context).void largerThan(MOBILE);
  // ResponsiveBreakpoints.of(context).void smallerThan(TABLET);
  // ResponsiveBreakpoints.of(context).void between(MOBILE, TABLET);
  // }
}
